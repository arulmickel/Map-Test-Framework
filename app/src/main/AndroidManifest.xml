<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 
    PERMISSIONS FOR A MAPS APP
    
    TESTING RELEVANCE:
    Permission handling is a MAJOR test scenario for MapKit SDET.
    - What happens when user grants location permission?
    - What happens when user denies it?
    - What happens when user revokes it mid-session?
    - Android 13-15 each handle permissions differently
    
    In tests, we use GrantPermissionRule or UiAutomator to simulate
    grant/deny flows.
    -->
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 10+ (API 29): required for geofencing while the app is not
         in the foreground. Prompted separately from fine location. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Android 13+ (API 33): POST_NOTIFICATIONS became a runtime permission.
         Below API 33 this declaration is ignored. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ACCESS_MOCK_LOCATION lives in src/debug/AndroidManifest.xml so it
         only ships to debug builds — release APKs never request it, which
         is the lint-recommended (and Play Store friendly) approach. -->

    <application
        android:name=".MapTestApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="MapTest"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapTest">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

    </application>

</manifest>
